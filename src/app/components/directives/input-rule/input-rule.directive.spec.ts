import { InputRuleDirective } from './input-rule.directive';

describe('InputRuleDirective', () => {
  it('should create an instance', () => {
    const directive = new InputRuleDirective();
    expect(directive).toBeTruthy();
  });
});
