import { InisialNamePipe } from './inisial-name.pipe';

describe('InisialNamePipe', () => {
  it('create an instance', () => {
    const pipe = new InisialNamePipe();
    expect(pipe).toBeTruthy();
  });
});
